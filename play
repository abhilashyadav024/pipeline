pipeline {
    agent any

    stages {
        stage('ek number') {
            steps {
                echo 'newzealand'
            }
        }
    }
}

pipeline {
      agent any

      stages {
         stage ('one') {
           steps {
              sh 'kotha syntax'
           }
         }
      }
}

